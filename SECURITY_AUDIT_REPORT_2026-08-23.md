# تقرير التدقيق الأمني — AI Debt OS

التاريخ: 2026-08-23
النطاق: كود Next.js 16، ترحيلات Supabase/PostgreSQL، WAHA، n8n، PM2/Hostinger، إعدادات CI/CD، والتقرير الخارجي المؤرخ 2026-08-18.

## الخلاصة التنفيذية

كانت حالة الكود قبل المعالجة **عالية الخطورة وغير جاهزة للإنتاج** بسبب أسرار متتبعة في Git، بحث وارد عبر الشركات بدون عزل كافٍ، تعطيل تحقق هوية العميل، عدم فرض MFA داخل واجهات API المميزة، SSRF في عناوين التكاملات، وعدم اتساق النشر وقاعدة البيانات.

تمت معالجة عيوب الكود المؤكدة وإضافة اختبارات وبوابات نشر. مع ذلك، لا يجوز اعتبار بيئة الإنتاج آمنة أو جاهزة حتى تُنفذ الخطوات الخارجية المتبقية: تدوير جميع الأسرار التي ظهرت في Git، تنظيف تاريخ Git بعد التدوير، تطبيق migration 057، وضبط متغيرات البيئة على خادم جديد. خادم Hostinger السابق متوقف، لذلك لم يمكن نشر التغييرات أو اختبار WAHA/n8n/Supabase إنتاجياً.

## نتائج التقرير الخارجي والتحقق

| الرمز | الخطورة | النتيجة | حالة المعالجة في الكود |
|---|---:|---|---|
| F-01 | P0 | الاتصال الصوتي الحقيقي غير منفذ | عُطلت أعلام ومدة Voice في الخطط وأزيل الرابط من القائمة؛ الصفحة المتبقية توضح أنها غير تشغيلية |
| F-02 | P0 | أسرار وملفات QR/Cookie متتبعة | أزيلت الملفات من الشجرة الحالية، توسع ماسح الأسرار، وأضيف فحص Gitleaks للتاريخ؛ التدوير وتنظيف التاريخ ما زالا مطلوبين |
| F-03 | P0 | مطابقة هاتف واردة عالمياً | أصبحت جلسة WAHA تُحل إلى شركة واحدة قبل أي استعلام، وكل مسارات العميل/جهات الاتصال/المتصل المجهول مقيدة بـcompany_id |
| F-04 | P0 | تحقق الهوية معطل | أُعيدت بوابة آخر أربعة أرقام قبل أي إفصاح، مع محاولتين وقفل وتحويل بشري وفشل مغلق |
| F-05 | P1 | MFA غير مفروض في API | `withAuth` يفرض AAL2 افتراضياً على admin/manager ويفشل مغلقاً عند تعذر قراءة مستوى الضمان |
| F-06 | P1 | قراءة إعدادات التكامل واسعة داخل الشركة | migration 057 يجعل SELECT لإعدادات التكامل مقتصراً على admin |
| F-07 | P1 | أسماء التكاملات في UI لا تطابق قيد DB | migration 057 يوحد القيم: waha وn8n_automation وcollection_api وtameez_calls وrasf_whatsapp |
| F-08 | P1 | SSRF عبر عناوين التكامل | أضيف HTTPS + allowlist + فحص DNS والعناوين الخاصة/metadata + إعادة تحقق من التحويلات ومنع تحويلات cross-origin |
| F-09 | P2 | نشر وفحص أسرار غير متسقين | أزيل نشر Vercel، أضيف master إلى CI، أصبحت الفحوص حاجبة، وأصبحت وثائق ونصوص النشر خاصة بـHostinger/PM2/WAHA/n8n |

## نتائج إضافية اكتشفت أثناء التدقيق

1. ملفات n8n/Evolution قديمة تضمنت كلمة مرور ومفتاحاً وعنوان VPS صريحاً؛ أزيلت ووسع الماسح لالتقاط كلمات المرور والعبارات عالية العشوائية.
2. نسخة مشروع متداخلة قديمة `ai-debt-os/` كانت مستبعدة من الفحوص لكنها تُشحن في النشر؛ أزيلت من التتبع وأضيفت إلى `.gitignore`.
3. webhook البريد كان يطابق البريد عالمياً؛ أصبح يتطلب عنوان استقبال مربوطاً بشركة عبر `EMAIL_INBOUND_COMPANY_MAP` ثم يقيد الاستعلام بالشركة.
4. عرض QR الخاص بـMFA استخدم HTML مباشراً؛ استبدل بصورة data URI بدون `dangerouslySetInnerHTML`.
5. إنشاء مستخدم مباشر من Supabase، إن أعيد تفعيله خطأ، كان يستطيع تمرير role في metadata؛ migration 057 يجبر الحساب الجديد على collector غير نشط حتى تفعله قناة إدارية موثوقة.
6. أضيفت HSTS وPermissions-Policy وسياسة CSP جزئية تمنع object/base/frame/form abuse، مع إبقاء رؤوس nosniff وDENY وReferrer-Policy.

## التحقق الآلي والديناميكي

- TypeScript: ناجح.
- اختبارات Vitest: **568/568 ناجحة** ضمن 65 ملفاً.
- بناء Next.js **16.3.2**: ناجح، 103 صفحات.
- ESLint الحاجب (`--quiet`): صفر أخطاء. ما زالت **501 ملاحظة legacy** ظاهرة وغير مخفية، أغلبها `any` ومتغيرات غير مستخدمة وأنماط React تحتاج تنظيفاً مرحلياً.
- migrations: **57 ملفاً متسلسلاً** وناجح التحقق.
- ماسح الأسرار في الشجرة الحالية: ناجح عبر 518 ملفاً متتبعاً.
- `npm audit` للإنتاج والتطوير: **0 ثغرات معروفة** وقت الفحص.
- اختبار وصول محلي على build الإنتاجي:
  - `/dashboard/admin` بدون جلسة: 307 إلى login.
  - `/register`: 404.
  - `/api/integrations/test` بدون جلسة: 401.
  - cron بدون secret: 401.
  - inbound email بدون إعداد secret: 503 (مغلق).
- اختبار حمل محلي محدود لمسار login: 500 طلب، concurrency 50، نجاح 500/500، نحو 143.9 طلب/ثانية، p50=326ms، p95=479ms، p99=606ms. هذا اختبار لعملية Node المحلية فقط وليس إثبات سعة قاعدة البيانات أو WAHA أو n8n أو الخادم الإنتاجي.

## المتبقي قبل الإنتاج — حواجز إلزامية

1. تدوير مفاتيح Supabase وWAHA وn8n وأي حساب/مفتاح ظهر في تاريخ Git، ثم تحديث البيئة الجديدة.
2. بعد التدوير، تنظيف تاريخ Git بتنسيق صريح وforce-push مخطط؛ الحذف من HEAD لا يبطل الأسرار القديمة.
3. تطبيق `supabase/migrations/057_security_audit_remediation.sql` والتحقق من السياسات والقيود على قاعدة البيانات الفعلية.
4. ضبط `WAHA_SESSION_COMPANY_MAP` و`INTEGRATION_ALLOWED_HOSTS`، وضبط `EMAIL_INBOUND_COMPANY_MAP` فقط إذا فُعل البريد.
5. نشر الكود على VPS جديد، حماية منافذ Next/WAHA/n8n/PostgreSQL، ثم اختبار TLS/Nginx/PM2/firewall والنسخ الاحتياطية.
6. إجراء اختبار عزل حي بحسابين وشركتين وبيانات متشابهة، واختبار تحميل موزع يشمل Supabase وWAHA والنقاط المكلفة. لا ينفذ اختبار ضغط على الإنتاج دون نافذة وحدود ومراقبة.
7. استكمال تنظيف 501 تحذيراً وفق baseline متناقص؛ لا تعد ثغرات مؤكدة لكنها دليل أن الكود ليس نظيفاً بالكامل بعد.

## القرار

**الكود المعالج قابل للبناء والاختبار، لكنه ليس مصرحاً للإنتاج بعد.** حالة No-Go تبقى قائمة على البيئة حتى تنفيذ تدوير الأسرار، migration 057، الإعدادات الجديدة، والنشر/التحقق الحي.
